Need to run /litprog-skill. We need to rewrite the literary program, not just update it. Use `surf` to review https://yinshi.io/architecture.html - we want to keep the same layout. It should look exactly the same as it does now, but the content of the literary program should be updated.

---

Need to run /litprog-skill. We need to rewrite the literary program, not just update it. Use `surf` to review https://yinshi.io/architecture.html - we want to keep the same layout. It should look exactly the same as it does now, but the content of the literary program should be updated.

---

Need to run /litprog-skill. We need to rewrite the literary program, not just update it. Use `surf` to review https://yinshi.io/architecture.html - we want to keep the same layout. It should look exactly the same as it does now, but the content of the literary program should be updated.